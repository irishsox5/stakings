<!-- Responsive panel example-->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Tour #<%= @tour.id %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">
              <dt>Name</dt>
              <dd><%= @tour.name %></dd>

              <dt>Image</dt>
              <dd><%= @tour.image %></dd>

            </dl>
            <div class="btn-group btn-group-justified">
              <%= link_to icon('chevron-left'), tours_path, class: "btn btn-primary" %>

              <%= link_to icon('edit'), edit_tour_path(@tour), class: "btn btn-warning" %>

              <%= link_to icon('trash-o'), @tour, method: :delete, data: { confirm: "Do you really want to delete this tour?" }, class: "btn btn-danger" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->

